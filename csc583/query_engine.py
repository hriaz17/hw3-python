from csc583.document import Document

class QueryEngine:

    def __init__(self,input_file):
        # add your code here
        pass

    def read_txt_file(self,input_file):
        # add your code here
        docs=None
        return docs

    def q1_1(self, query):
        # This is just sample code. Add your actual code here.
        # The Document class we provided is just a dummy wrapper over Lucene document.
        # the document you use must be the Lucene document i.e doc = document.Document()
        ans=[]
        # first argument must be of type document.Document()
        return ans

    def q1_2_a(self, query):
        # This is just sample code. add your actual code here.
        # The Document class we provided is just a dummy wrapper over Lucene document.
        # the document you use must be the Lucene document i.e doc = document.Document()
        ans=[]
        # first argument must be of type document.Document()
        return ans


    def q1_2_b(self, query):
        # This is just sample code. add your actual code here.
        # The Document class we provided is just a dummy wrapper over Lucene document.
        # the document you use must be the Lucene document i.e doc = document.Document()
        ans =[]
        # first argument must be of type document.Document()
        return ans

    def q1_2_c(self, query):
        # This is just sample code. add your actual code here.
        # The Document class we provided is just a dummy wrapper over Lucene document.
        # the document you use must be the Lucene document i.e doc = document.Document()
        ans =[]
        # first argument must be of type document.Document()
        return ans

    def q1_3(self, query):
        # This is just sample code. add your actual code here.
        # The Document class we provided is just a dummy wrapper over Lucene document.
        # the document you use must be the Lucene document i.e doc = document.Document()
        ans =[]
        # first argument must be of type document.Document()
        return ans

